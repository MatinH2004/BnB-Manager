<header>
  <h2>Property: <%= @apartment[0][:apartment_name] %></h2>
  <p><b><%= @apartment[0][:apartment_address] %></b></p>
  <a href="/login">Login</a><br>
  <a href="/signup">Signup</a>
</header>

<table>
  <tr>
    <th>Name</th>
    <th>Rent</th>
    <th>Edit</th>
    <th>Delete</th>
  </tr>

  <% if no_tenants? %>
    <tr>
      <td>No Data</td>
      <td>No Data</td>
      <td>No Data</td>
      <td>No Data</td>
    </tr>
  <% end %>

  <% @apartment.each do |tenant| %>
    <% break if no_tenants? %>
    <tr>
      <td><%= tenant[:tenant_name] %></td>
      <td><%= tenant[:tenant_rent] %></td>
      <td>
        <a href="/edit/<%= tenant[:apartment_id] %>/tenant/<%= tenant[:tenant_id] %>">Edit</a>
      </td>
      <td>
        <form method="post" action="/delete/<%= tenant[:apartment_id] %>/tenant/<%= tenant[:tenant_id] %>">
          <button type="submit">Delete</button>
        </form>
      </td>
    </tr>
  <% end %>
</table>

<footer>
  <a href="/new/<%= @apartment[0][:apartment_id] %>/tenant">New Tenant</a><br>
  <a href="/">Main Menu</a>
</footer>